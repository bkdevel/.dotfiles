cowsay -f stegosaurus "Willkomen, $USER, in der tcsh auf `lsb_release -d | cut -f2-`"
fortune


## tcsh propmpt
set prompt = "%n@%m: < %~ > % "

## environment vars
setenv EDITOR vim
setenv VISUAL vim
setenv PAGER  bat
setenv SHELL  tcsh


#
## aliases
#

alias l  "ls -l --color"
alias la "ls -la --color"
alias al "ls -la --color"
alias ls "ls --color"

alias sysfetch "fastfetch"
alias fetch "fastfetch"

alias gnuplot "gnuplot -p"
